main {
    string file = "./binaries/setup.exe";
    PEFile pe = peopen(file);
    int arch = pe.file_header.machine;
    if (arch == 267) { 
        print "32";
    } else if (arch == 523) { 
        print "64";
    }
    
}